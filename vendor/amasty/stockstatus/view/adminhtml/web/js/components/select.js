define([
    'underscore',
    'Magento_Ui/js/form/element/select',
    'Magento_Ui/js/lib/validation/validator',
    'uiRegistry'
], function (_, select, validator, uiRegistry) {
    return select.extend({
        defaults: {
        }
    });
});
